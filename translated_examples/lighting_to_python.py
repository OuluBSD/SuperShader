import numpy as np

def calculate_diffuse_lighting(np.ndarray normal, np.ndarray light_dir, np.ndarray light_color):
    intensity = None  # : float = max(dot(normal, light_dir), 0.0);
    diffuse = None  # : vec3 = intensity * light_color;
    return diffuse
}